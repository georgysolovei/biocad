apiVersion: v1
kind: Service
metadata:
  name: biocad-hello
  labels:
    app: biocad-hello
spec:
  type: ClusterIP
  selector:
    app: biocad-hello
  ports:
    - name: http
      port: 32777
      targetPort: 32777
      protocol: TCP